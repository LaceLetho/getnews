- 2026-04-01: `MainController` already had `RepositoryFactory` wiring (`analysis_repository` / `ingestion_repository` / `content_repository` / `cache_repository`), but business flow still called `data_manager` and `cache_manager` directly in crawl/analyze/manual-report paths. Task 4 boundary cleanup should prioritize swapping those call sites to repository methods instead of introducing a parallel abstraction.
- 2026-04-01: Existing `storage/repositories.py` can be safely extended with transition helpers (`save_many`, `get_recent_content_items`, `get_content_items_since`, `save_crawl_status`, `cache_sent_messages`, `get_cache_statistics`, `log_execution`) while keeping SQLite behavior unchanged through wrapped `DataManager`/`SentMessageCacheManager`.
- 2026-04-01: After swapping MainController to repository calls, the shared contract in `domain/repositories.py` must be expanded in lockstep; otherwise abstraction is only concrete-level and future backend substitution stays unsafe.
- 2026-04-02: Phase 1 ingestion failure (`LLM API密钥不能为空`) root cause was not ingestion runtime initialization itself, but `_execute_crawling_stage` calling `ConfigManager.get_auth_config()` just to read X credentials; that method constructs `AuthConfig` and enforces analysis-side LLM/Telegram fields. Ingestion-safe fix is to read X-only credentials via a dedicated config path (`get_x_auth_credentials`) so crawl can run without LLM keys while analysis paths remain unchanged.
- 2026-04-03: Telegram sender leak fix stayed local by redacting exception text inside `crypto_news_analyzer/reporters/telegram_sender.py` instead of changing shared logging utilities; this preserved request flow while preventing bot tokens from reaching warning/error logs and returned error messages.
- 2026-04-03: For the global leak fix, installing redaction at the `LogRecordFactory` layer in `crypto_news_analyzer/utils/logging.py` is more reliable than a root logger filter because child logger records from third-party libraries propagate straight to handlers.
