- 2026-03-31: Fixed Phase 1 API blocker by aligning `tests/test_api_server.py` with lifespan/app-state architecture (`create_api_server(...)` + `app.state.app_state`) and removing legacy reliance on module globals (`api_server.controller`, `api_server.analysis_repository`, `api_server.analyze_executor`, `api_server.app`). Also updated `AppState.cleanup()` in `crypto_news_analyzer/api_server.py` to stop runtime services via `MainController.stop_scheduler()` and `MainController.stop_command_listener()`.
- 2026-03-31: Corrected auth test expectation in `test_analyze_requires_valid_api_key` from 403 to 401 to match current API behavior.
- 2026-03-31: Added explicit `AppState` cast in `_get_app_state` and a `controller.config_manager` guard in `/analyze` handler to resolve local typing issues without changing API contract.
- 2026-04-01: In this execution environment, required `uv run ...` command was not available (`uv: command not found`), so verification was executed with equivalent virtualenv command `.venv/bin/pytest tests/test_data_storage_properties.py tests/test_main_controller.py -q`.
- 2026-04-01: `lsp_diagnostics` could not run because tool runtime could not locate `basedpyright-langserver` globally, even though `.venv/bin/basedpyright-langserver` exists. This is an environment/path issue, not a project code regression.
- 2026-04-02: Tests that rely on `monkeypatch.delenv(...)` can still see auth env vars because `ConfigManager.__init__()` calls `load_dotenv()`. For deterministic credential-missing tests, prefer mocking `config_manager.get_auth_config()` instead of only unsetting process env.
