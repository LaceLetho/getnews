
20. - Task 10's Telegram `rest_api` add flow works by removing the temporary type gate in `_handle_datasource_add_command()` while keeping the shared `validate_telegram_datasource_create_payload()` path intact. The secret rejection already happens in `datasource_payloads.py` through `_reject_telegram_rest_api_inline_auth()` and `_reject_telegram_inline_secrets()` helpers.
21. - The shared validator's auth-before-payload pattern means Telegram `rest_api` payloads get inspected for secrets twice: once at the raw `config_payload` level before shared validation, and again after normalization in `validate_datasource_create_payload()`. This is intentional for defense in depth.
22. - Cross-surface visibility tests can verify a datasource created via Telegram appears in REST list output by checking the repository directly (since both surfaces share the same repository instance). This avoids needing a full HTTP client round-trip while still proving the integration.
23. - Telegram command help text needs exact JSON examples without newlines or Markdown formatting inside the JSON, because the Telegram UI renders the help text as plain text and malformed JSON in examples confuses users.
24. - The `_setup_bot_commands()` descriptions are visible to all users in the Telegram bot menu, so removing "开发中" (in development) is an important UX signal that the feature is now production-ready.
25. - The safest narrow regression for the old SQLite `INSERT OR REPLACE` uniqueness bug is to simulate a stale pre-check by monkeypatching `SQLiteDataSourceRepository.get_by_type_and_name()` to miss an already-existing row, then asserting the duplicate save still fails and the original datasource row remains unchanged.
26. - `execution_coordinator._execute_crawling_stage()` can cover `rest_api` without widening the crawl-status contract by reusing the existing `DataSourceFactory` boundary and appending the crawled items into `all_content_items`, while leaving the persisted `CrawlStatus` shape (`rss_results`/`x_results`) untouched.
27. - The REST datasource create contract distinguishes structural request validation from semantic datasource validation: malformed request bodies still come from FastAPI as `422`, and datasource semantic validation should also return `422` once `DataSourcePayloadValidationError` is mapped consistently in `api_server.create_datasource()`.
28. - Final verification rerun on 2026-04-06 showed the datasource-focused regression set already passing end-to-end: shared tag count/length validation, runtime `rest_api` crawling, REST semantic `422` responses, and atomic duplicate conflict handling were all covered by the existing tests and current implementation.
29. - The required verification commands needed an environment bootstrap first because `uv` was missing from the shell; installing `uv` to `/data/.local/bin/uv` was sufficient to run the repo's prescribed pytest commands without changing datasource code.
30. - Telegram `/datasource_add` should treat `DataSourceAlreadyExistsError` as the primary duplicate contract and only fall back to legacy `ValueError` text matching for compatibility; the duplicate reply stays the same, but the handler now matches the repository’s current exception shape directly.
31. - The shared `SQLiteContentRepository.get_content_items_since()` wrapper should pass `max_hours` through unchanged. The underlying `DataManager` already owns the default window, so adding a second default in the repository layer was unnecessary scope creep.
32. - The `AnalysisRequest.from_dict()` / `IngestionJob.from_dict()` datetime parsing should stay at the shared-model boundary only; reverting the helper-based required-datetime path keeps datasource work isolated without changing the observable datasource behavior.
