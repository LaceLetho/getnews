# 加密货币新闻分析工具 - Docker环境变量模板
# 复制此文件为 .env.docker 并填入实际配置值

# =============================================================================
# 基础配置
# =============================================================================

# 时间窗口（小时）- 只分析指定时间窗口内的内容
TIME_WINDOW_HOURS=24

# 执行间隔（秒）- 定时调度模式下的执行间隔
# 3600 = 1小时, 21600 = 6小时, 43200 = 12小时, 86400 = 24小时
EXECUTION_INTERVAL=21600

# 配置文件路径
CONFIG_PATH=/app/config.json

# =============================================================================
# 认证配置
# =============================================================================

# LLM API密钥（必需）
# 支持 OpenAI GPT, MiniMax 等兼容 OpenAI API 的服务
llm_api_key=your_llm_api_key_here

# Telegram Bot配置（必需）
# 从 @BotFather 获取 Bot Token
telegram_bot_token=your_telegram_bot_token_here

# Telegram频道ID（必需）
# 可以是 @channel_username 或数字ID（如 -1001234567890）
telegram_channel_id=your_telegram_channel_id_here

# X/Twitter认证（可选）
# 从浏览器开发者工具获取
x_ct0=your_x_ct0_token_here
x_auth_token=your_x_auth_token_here

# =============================================================================
# 调试和日志配置
# =============================================================================

# 启用调试模式
DEBUG=false

# Python日志级别
PYTHONLOGLEVEL=INFO

# =============================================================================
# 容器配置
# =============================================================================

# 容器时区
TZ=Asia/Shanghai

# 容器用户ID（可选，用于权限管理）
# USER_ID=1001
# GROUP_ID=1001

# =============================================================================
# 存储配置
# =============================================================================

# 数据保留天数
RETENTION_DAYS=30

# 最大存储大小（MB）
MAX_STORAGE_MB=1000

# =============================================================================
# 高级配置
# =============================================================================

# LLM模型名称
LLM_MODEL=gpt-4

# LLM温度参数
LLM_TEMPERATURE=0.1

# LLM最大令牌数
LLM_MAX_TOKENS=1000

# 批处理大小
LLM_BATCH_SIZE=10

# =============================================================================
# 网络配置
# =============================================================================

# HTTP超时（秒）
HTTP_TIMEOUT=30

# 重试次数
MAX_RETRIES=3

# 重试延迟（秒）
RETRY_DELAY=5

# =============================================================================
# 使用说明
# =============================================================================
# 
# 1. 复制此文件为 .env.docker
# 2. 填入实际的配置值
# 3. 使用 docker-compose 启动容器：
#    - 一次性执行: docker-compose run --rm crypto-analyzer once
#    - 定时调度: docker-compose up -d crypto-analyzer-scheduler
# 
# 注意：
# - LLM_API_KEY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHANNEL_ID 是必需的
# - X认证信息是可选的，如果不提供将跳过X/Twitter数据源
# - 所有时间相关的配置都使用秒为单位
# - 调试模式会输出更详细的日志信息
#