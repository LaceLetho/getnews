
20. - Task 10 removes the explicit `rest_api` type gate from `_handle_datasource_add_command()`, allowing Telegram to create `rest_api` datasources through the same shared validation path as `rss` and `x`. Secret rejection is preserved through the existing `validate_telegram_datasource_create_payload()` function which inspects raw `config_payload` before shared normalization.
21. - The bot command menu descriptions for `/datasource_add` and `/datasource_delete` are updated to remove the "（开发中）" suffix, signaling these features are now production-ready.
22. - The `/help` text now includes exact JSON examples for all three datasource types (`rss`, `x`, `rest_api`) with plain-text formatting suitable for Telegram's message rendering. The examples demonstrate the complete payload structure including required fields and normalized tag arrays.
23. - Cross-surface visibility is verified by tests that create a datasource via Telegram command, then assert it appears in both the Telegram `/datasource_list` output and the repository's `list()` method (which backs REST `GET /datasources`).
24. - Duplicate datasource creation is now enforced atomically at the storage/repository boundary instead of via a read-before-write precheck: `DataManager` uses `ON CONFLICT(id) DO UPDATE` for both SQLite and Postgres, and `SQLiteDataSourceRepository.save()` translates datasource unique-index violations into a dedicated duplicate-domain error that REST/Telegram surfaces can map to conflict responses.
25. - Tag count and tag length limits are enforced in `validate_datasource_create_payload()` through shared datasource tag validation so both REST and Telegram datasource creation paths inherit the same normalized constraints.
26. - `rest_api` runtime crawling is wired into `_execute_crawling_stage()` by loading `config_manager.get_rest_api_sources()` and calling `factory.create_source("rest_api", time_window_hours)` for each source, preserving the existing RSS/X loops and avoiding any crawl-status schema redesign.
27. - No datasource code changes were applied in this pass because the current branch already satisfies the four final-wave rejection items and the required datasource verification suite passes unchanged; the only session writeback was appending the verification outcome to the datasource task notepads.
28. - Telegram duplicate datasource creation is now handled by the repository's concrete `DataSourceAlreadyExistsError` contract instead of by message substring inspection alone. The legacy `ValueError` fallback remains only as a compatibility guard.
29. - The off-scope shared-code edits were reverted in place: `domain/models.py` keeps the older optional datetime deserialization path, and `SQLiteContentRepository.get_content_items_since()` no longer injects its own `24h` default.
30. - The Telegram `/help` handler now sends plain text and the help body itself is kept plain-text friendly, so the existing raw JSON datasource examples remain readable without risking Telegram Markdown entity parse failures.
31. - Datasource list enrichment stays entirely in `telegram_command_handler.py` by deriving extra presentation lines from stored `config_payload` values instead of changing datasource models, repositories, or REST contracts.
32. - `/datasource_add` input and validation failures now append concise type-specific guidance plus an explicit `/help` pointer in the handler layer, while duplicate and delete-business outcomes are logged as expected info-level results rather than surfacing as generic error-path noise.
33. - The logging fix is intentionally narrow: `uvicorn.access` records bypass the redacting `LogRecordFactory`, so access formatting can keep unpackable tuple args unchanged. Application logs still flow through the existing Telegram-sensitive redaction path.
34. - Documentation changes remain strictly within the required files (AGENTS.md, README.md, docs/RAILWAY_DEPLOYMENT.md, migrations/postgresql/README.md) and do not modify source code, tests, or plan files. The AGENTS.md now contains a dedicated "Datasource Runtime Behavior" section explaining database-first storage, bootstrap behavior, tag constraints, REST endpoints, and Telegram restrictions.
